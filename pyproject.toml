[project]
name = "finpipe"
version = "0.1.0"
description = "Financial data platform — backend API, data ingestion, orchestration, and frontend"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["backend", "dagster", "common"]
