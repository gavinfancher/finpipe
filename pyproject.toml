[project]
name = "finpipe"
version = "0.1.0"
description = "Financial data platform — pipelines, streaming, and UI"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["dagster", "deploy", "stream"]
